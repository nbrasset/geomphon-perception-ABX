
1.BY HAND- LMEDS update
	update LMEDs dictionary (with new experiment code, if nothing else)
	(if necessary) update sequence
		update sequence file 
			change all sound files to new soundfiles 
				audio_test
				practice 
				experiment files 
	(if necessary, change surveys)
	(if necessary) update surveys
		 IF update, then update cleaning script by hand
	update .cgi file 
		change file name 
		change name of experiment 
		(if necessary, change dict and sequence)
		(if necessary, change randomization and relogin=true)


2. Push updates to server 

3. Update server permissions to allow experiment to run 
